const CONFIG_AR = {
    componenteId: 'inicio-equipo-sobremesa',

    escalaInicial: 3.00,
    escalaMinima: 0.005,

    // Cada pulsación en + o - cambia este valor.
    // Aumenta este número para que el modelo cambie de tamaño más rápido.
    pasoEscala: 0.05,

    posicionInicial: {
        x: 0,
        y: 0.2,
        z: 0
    },

    rotacionInicial: {
        x: 0,
        y: 0,
        z: 0
    }
};